{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "bash -lc 'echo \"$GOOGLE_CREDENTIALS_JSON_B64\" | base64 -d > credentials.json && echo \"$GOOGLE_TOKEN_JSON_B64\" | base64 -d > token.json && python -m app.main'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
